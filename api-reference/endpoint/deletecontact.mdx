---
title: "Delete Contact"
openapi: "DELETE /api/v2/contacts/{contact}"
sidebarTitle: "Delete Contact"
description: "This endpoint provides a command to delete an existing contact. Users can delete the contact handle if there are no active domains assigned to it. If there are any active domains associated with the contact handle, users must first remove them before deleting the contact."
---