---
title: "Change Transfer Lock for Domain Name"
openapi: "PATCH /api/v2/domains/{domain}/transfer_lock"
sidebarTitle: "Change Transfer Lock for Domain Name"
description: "This endpoint provides a command to change the Transfer Lock status for a domain name. The Transfer Lock status is a setting that can prevent a domain name from being transferred to a different registrar."
---